import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Loading the 3D Desktop Engine
import './index.css' // Loading the Swarm styling

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
